#!/bin/bash
# Install with:
#   git config core.hooksPath ai-agent-core/macos_agent/.githooks
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT/ai-agent-core/macos_agent"
python3 tools/lint_invariants.py
